export class Gift {

  name: string;
  url: string;
  child: string;


  constructor(name: string, url: string, child: string) {
    this.name = name;
    this.url = url;
    this.child = child;
  }
}
